<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sanmei.dao.personal.PersonalMyPageDao">

    <select id="countUser" resultType="Integer">
        SELECT count(0)
        FROM sys_user u
        WHERE u.delete_status = '1'
    </select>

    <select id="getCurUsers" resultType="com.sanmei.model.sysUser.SysUser" parameterType="java.lang.String">
        SELECT
            a.id          id,
            a.username    userName,
            a.role_id     roldId,
            b.role_name   roleName,
            a.sex         sex
        FROM sys_user a left join sys_role b on a.role_id = b.id
        WHERE a.delete_status='1' and a.id=#{userId,jdbcType=VARCHAR}
        limit 1
    </select>

</mapper>